/** @type {import('prettier').Config} */
const config = {
  tabWidth: 2,
  useTabs: false,
  resolveGlobalModules: true,
  semi: false,
  singleQuote: true,
  trailingComma: 'all',
  arrowParens: 'avoid',
  endOfLine: 'lf',
}

module.exports = config
